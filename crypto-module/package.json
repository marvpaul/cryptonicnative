{
  "name": "crypto-module",
  "version": "1.3.7",
  "description": "This module offers a an interface between some cipher methods of nodes built-in crypto library and nativescript as well as offering the same interface for some self implemented ciphers, e.g. caesar cipher.",
  "main": "index.js",
  "scripts": {
    "test": " mkdir test && cp test.js test/ && cd test && npm --cache-min Infinity install ../ && node test.js && cd .. && rm -rf test && exit 0"
  },
  "author": "Marvin Krüger",
  "license": "ISC",
  "dependencies": {
    "cipherjs": "*",
    "egoroof-blowfish": "^2.2.0",
    "nativescript-nodeify": "*",
    "scryptsy": "^2.0.0"
  }
}
